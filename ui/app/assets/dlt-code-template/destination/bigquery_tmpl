import dlt

config = {
    "project_id": "<project_id>",
    "private_key": "<private_key>",
    "client_email": "<client_email>",
    "location": "<location>"
}

pipeline = dlt.pipeline(
    pipeline_name=__current.PIPELINE_NAME,
    destination=dlt.destinations.bigquery(credentials=config),
    dataset_name="<dataset_name>",
)