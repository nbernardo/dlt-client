%metadata_section%import dlt
from dlt.sources.credentials import ConnectionStringCredentials
from dlt.sources.sql_database import sql_database, sql_table
from sys import path
%import_from_src%
from src.utils.pipeline.PipelinesHelper import PipelineLogger, PipelineHelper as Helper

logger = PipelineLogger()
logger.info("Starting SQL database pipeline", extra={'stage': 'initialization', 'template_type': 'sql_db'})

from src.services.workspace.Workspace import Workspace
from src.services.workspace.SecretManager import SecretManager
from src.utils.SQLDatabase import additional_parse, convert_fields_type

# Bellow mapping: namespace = SqlDBComponent.namespace
namespace, original_pipeline_name = %namespace%, '%original_pipeline_name%'

# Bellow mapping: connection_name = SqlDBComponent.connection_name
connection_name = %connection_name%

print(f'fetching "{connection_name}" secrets for DB connection', flush=True)
logger.info("Fetching database connection secrets", extra={'stage': 'authentication', 'namespace': namespace, 'connection_name': connection_name})
secret = SecretManager.get_db_secret_from_ppline(namespace, connection_name)

connection_string = secret['connection_url']
connection_params = f'?sslmode=prefer' if secret['dbengine'] == 'postgresql' else ''

# Bellow mapping: source_database = SqlDBComponent.source_database
database_to_connect = %source_database%
logger.info("Establishing database connection", extra={'stage': 'database_connection', 'database': database_to_connect})

credentials, additionals = ConnectionStringCredentials(f'{connection_string}{connection_params}'), {}

def load_select_tables_from_db():
    dest_folder = Workspace.get_duckdb_path_on_ppline()
    %destination_settings%

    # Bellow mapping: original_tables = SqlDBComponent.source_tables
    original_tables = %source_tables%

    # Bellow mapping: pks = SqlDBComponent.primary_keys
    original_tables_pk = %primary_keys%

    # Bellow mapping: increment_by_fields = SqlDBComponent.increment_by_fields
    increment_by_fields = %increment_by_fields%
    
    pplines = { 'analytics_storage': ppline_name, 'ppline_name': original_pipeline_name }

    logger.info("Normalizing table names", extra={'stage': 'data_preparation', 'table_count': len(original_tables)})
    tables, pks, rels, meta, ddls, big_query, time_stmp = additional_parse(secret, original_tables, original_tables_pk, pplines)
    additionals = { 'namespace': namespace, 'db_name': %output_dest_name%, 'tbls': tables, 'rels': rels, 'stage': %stage%, 'ts': time_stmp, 'pks': pks }
    additionals = { **additionals, 'meta': meta, 'ddls': ddls, 'dest': dest, 'big_query': big_query, 'use_existing_dw': '%use_existing_dw%', **pplines }

    short_query = Helper.short_query(big_query)
    print(f'SHORT_QUERY=__e2e_short_query_:{short_query}')

    logger.info("Setting up data source", extra={'stage': 'data_source_set', 'table_count': len(tables), 'schema': %schema%})
    
    [source, tbl_stage] = [[], additionals.get('stage')]
    staged_table = tbl_stage in [2,3]

    for idx in range(len(tables)):
        table, schema, db_table = tables[idx], None, None
        if tables[idx].__contains__('.'):
            schema, table = tables[idx].split('.')
        logger.debug("Processing table w/ schema", extra={ 'stage': 'table_process', 'table': table, 'schema': schema, 'pk': pks[idx], 'tbl_count': idx + 1, 'total_tables': len(tables) })
        
        engine_kwargs = {'pool_pre_ping': True, 'pool_recycle': 1800} if secret['dbengine'] == 'postgresql' else {}
        if schema != '':
            db_table = sql_table(table=table,  credentials=credentials, schema=schema, engine_kwargs=engine_kwargs)
        else:
            db_table = sql_table(table=table,  credentials=credentials, engine_kwargs=engine_kwargs)

        db_table = convert_fields_type(db_table, pks[idx], additionals, original_pipeline_name, secret['database'])

        if schema != None and schema != '': db_table.apply_hints(table_name=f"{schema}_{table}")
        if staged_table: # In case the tables will be in stage area
            db_table.apply_hints(table_name=Helper.prefix_and_suffix_table(table, tbl_stage))

        if(len(increment_by_fields) > 0 and increment_by_fields[idx] != ''):
                db_table.apply_hints(incremental=dlt.sources.incremental(increment_by_fields[idx], on_cursor_value_missing='include'))

        print(f'Resource status {table}',flush=True)
        print(db_table,flush=True)
        print('',flush=True)

        source.append(db_table)

    print('Starting pipeline run', flush=True)
    logger.info("Starting pipeline execution", extra={'stage': 'pipeline_execution', 'table_count': len(tables)})
    
    info = pipeline.run(source, write_disposition='merge', %table_format%)
    
    if hasattr(info, 'loads_ids') and info.loads_ids:
        logger.info("SQL database pipeline execution completed successfully", extra={
            'stage': 'pipeline_completion', 'loads_ids': str(info.loads_ids), 'tbl_count': len(tables), 'dataset_name': getattr(info, 'dataset_name', 'unknown'), 'template_type': 'sql_db'
        })
    else:
        logger.info("SQL database pipeline execution completed", extra={ 'stage': 'pipeline_completion', 'tbl_count': len(tables), 'template_type': 'sql_db' })
    
    catalog_table_path = f'{secret['host']}:{secret['port']}/{secret['database']}'
    Helper.wrapup_run(catalog_table_path, src_path, pipeline, info, additionals)


load_select_tables_from_db()